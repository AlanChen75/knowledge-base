# 2026-04-21 工作日誌

## [12:00-13:24] cooperation.tw 首頁改版 + kb.cooperation.tw 上架

- **來源**: MacBook CLI
- **狀態**: ✅ 完成
- **目標**: 整併 ClassClaw 到 GemGate modal、重排 9 宮格、新增「知識學習系統」母卡、建 kb.cooperation.tw landing

### 執行步驟與結果

1. **盤點與釐清現況** — 發現 memory 過時 27 天
   - `ai-cooperation/ai-cooperation.github.io` (9 宮格 HTML) 才是 cooperation.tw 首頁
   - `ai-cooperation/cooperation.tw` (Quarto) 是內頁（/blog, /methodology 等）
   - 這兩個 repo 的分工容易搞混，已寫入 memory

2. **討論母卡策略** — 一應用一卡 vs 一張母卡多應用
   - 用戶本來要把保險/醫療/iPAS AI/淨零碳各自上架一張卡（9→15）
   - 建議走**母卡 + 獨立子站**：首頁精簡、品牌集中、SEO 各吃各的垂直詞
   - 用戶採納，定案 kb.cooperation.tw 為 master

3. **9 宮格改版**（ai-cooperation.github.io commit `fdb6aec`）
   - 位置 3：新增「知識學習系統」卡 → kb.cooperation.tw
   - 位置 8：法拍地圖（從位置 3 搬來）
   - 移除獨立 ClassClaw 卡
   - GemGate modal 從單按鈕變雙按鈕（GemGate + ClassClaw），改寫 `populateModal()` 支援 `links` 陣列
   - Hero h1 加 `<br>`：打造人機協作 / 的基礎架構

4. **kb 卡片縮圖** — MCP `generate_image` pro 模型，53.9s，深海軍藍 + 藍紫光暈 + 抽象 3D 知識圖譜（456x255，175K）

5. **kb.cooperation.tw landing 建立**
   - 用戶提供 `/Users/user/Desktop/AI100講/SecondBrain-slides.html` 當內容來源
   - 結構：Hero + 6 痛點 + 4 步方法論（骨架/視覺/編譯/自動化）+ 領域應用 + 開源生態（Obsidian/Graphify/Hermes/OpenClaw/Semantic Scholar/PubMed）+ 目標族群 + CTA
   - 用戶多輪修正：
     - 保險**無考試**，純市場資訊收集/WIKI 彙整（不要寫「考生」）
     - 所有 CTA 按鈕指向 `#applications` 而非直接保險
     - 不寫「4 個領域應用」（以後會擴充）
     - 「淨零碳規劃師」→「iPAS 淨零碳規劃師」
     - 「我們不是從零造輪子」段加 `<br>` 換行
   - 新 repo `ai-cooperation/kb-cooperation-tw`，GitHub Pages 部署

6. **部署 + DNS**
   - 9 宮格 push → GitHub Pages build → `cooperation.tw` HTTP 200 ✅
   - kb repo push → GitHub Pages build → github.io 301 → kb.cooperation.tw
   - 用戶去 Cloudflare 加 CNAME `kb` → `ai-cooperation.github.io`（DNS only 灰雲）
   - DNS 生效後 Let's Encrypt 自動簽發憑證
   - API 啟用 Enforce HTTPS：`gh api -X PUT /repos/ai-cooperation/kb-cooperation-tw/pages -F https_enforced=true`（坑：`-f` 傳字串不行，要 `-F` 傳 boolean）
   - 最終：https://kb.cooperation.tw/ HTTP/2 200，subject=CN=kb.cooperation.tw，issuer=Let's Encrypt ✅

### 變更檔案

**ai-cooperation/ai-cooperation.github.io**
- `index.html`（卡片重排、GemGate modal 雙按鈕、h1 換行、projectData 新增 kb）
- `images/proj-kb.png`（新增）

**ai-cooperation/kb-cooperation-tw**（新 repo）
- `index.html`（landing page，~450 行）
- `CNAME`（kb.cooperation.tw）
- `README.md`、`.gitignore`

**MacBook memory**
- `~/.claude/projects/-Users-user-projects-cooperation-tw/memory/project_overview.md` 重寫

### 關鍵決策記錄（避免未來返工）

- cooperation.tw 首頁 = `ai-cooperation.github.io` repo（9 宮格手寫 HTML），**不是** Quarto repo
- 子網域部署策略不統一：ai-cooperation.github.io / kb.cooperation.tw 用 **GitHub Pages**；insurance-kb / ai100 / aipath / paperlab 用 **Cloudflare Pages**
- Cloudflare DNS → GitHub Pages 必須「DNS only」(灰雲)，開 proxy 會卡 Let's Encrypt 簽發
- `gh api -X PUT` 設 boolean 要用 `-F`（typed）不能用 `-f`（string）
- MCP generate_image pro 產出 456x255 近似 16:9，適合 170px 高的卡片視覺區
