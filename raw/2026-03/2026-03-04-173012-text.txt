---
received: 2026-03-04 17:30:12
type: text
source: telegram
---

每次跟 OpenClaw 對話,它都要先載入一堆 context 檔案,光啟動就吃掉 13,190 tokens

壓縮完剩 6,673。每天省下來的費用不是重點,重點是我根本不知道有這個問題,直到我把它視覺化

方法很簡單,建了一個 Token Dashboard,掃描所有 context 檔案,顯示每個檔案佔多少 tokens、哪些是每次啟動必載的、哪些可以移到按需載入

看到數字的瞬間很震驚。AGENTS.md 吃了 4,355 tokens,裡面很多是我早就內化的規則,壓縮後剩 1,117。MEMORY.md 同理,4,648 → 1,700

還順手找到三個不該在 workspace 裡的巨型資料檔案,其中一個 27MB

這邊不藏私,直接分享給大家,以下是 Prompt,可以直接貼給你的龍蝦

「
幫我建立一個 Token 使用量 Dashboard,讓我能清楚看到所有活動消耗了多少 tokens。

我想了解的包含:每個 .md 檔案有多大、載入時消耗多少 tokens、每個 cron job 跑一次用多少 tokens、每次啟動新 session 消耗多少 tokens,以及過去 5 天的每日使用量(依 LLM 分類)。

我要一個可以瀏覽的 context 檔案目錄,能點開資料夾查看每個檔案的大小和 context 佔用量。

然後,審計我們的檔案結構和系統,找出在不損失功能的前提下優化 token 使用量的方法。

在執行任何變更之前,先給我計劃和建議清單讓我確認。
」

有幫助的話,別忘了點讚,並分享給需要的朋友～

你的 AI 每次對話燒多少錢,你知道嗎?
